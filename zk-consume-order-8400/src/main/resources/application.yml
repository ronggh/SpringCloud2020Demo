server:
  port: 8400
spring:
  application:
    name: zk-consume-order
  cloud:
    # 注册到ZK
    zookeeper:
      connect-string: localhost:2181
#      discovery:
#        enabled: true
#        instance-host: localhost
#        instance-port: ${server.port}
        #instance-id: ${spring.application.name}
